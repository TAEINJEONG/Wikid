import { AlarmIcon } from '@/components/common/Icon/Icon';

const WikiList = () => (
  <div className="flex gap-6 j items-center bg-white p-6 rounded-lg shadow-lg">
    <AlarmIcon size={24} className="cursor-pointer hover:scale-110 transition-transform" />
  </div>
);

export default WikiList;
